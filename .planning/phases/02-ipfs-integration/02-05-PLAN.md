---
phase: 02-ipfs-integration
plan: 05
type: execute
wave: 1
depends_on: [02-03, 02-04]
files_modified: [src/app/page.tsx]
autonomous: true
requirements: [IPFS-01, ACCESS-01, ACCESS-02]
gap_closure: true

must_haves:
  truths:
    - "Homepage displays upload section with Generate & Upload Mock EEG button and file upload input"
    - "Homepage displays access control section with researcher address input and Grant Access button"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Main page with UploadButton and AccessControl components rendered"
      contains: "UploadButton, AccessControl"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/UploadButton.tsx"
      via: "import and render"
      pattern: "import.*UploadButton"
    - from: "src/app/page.tsx"
      to: "src/components/AccessControl.tsx"
      via: "import and render"
      pattern: "import.*AccessControl"
---

<objective>
Integrate UploadButton and AccessControl components into the homepage to display the upload and access management UI.

Purpose: Close UAT gaps where homepage showed only wallet connect button with no upload or access control UI.
Output: page.tsx with both components imported and rendered.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-ipfs-integration/02-UAT.md
@src/app/page.tsx
@src/components/UploadButton.tsx
@src/components/AccessControl.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/UploadButton.tsx:
```typescript
interface UploadButtonProps {
  onUploadComplete?: (cid: string, url: string) => void
  userId?: string
}
export function UploadButton({ onUploadComplete, userId }: UploadButtonProps)
```

From src/components/AccessControl.tsx:
```typescript
interface AccessControlProps {
  onAccessGranted?: (address: string) => void
  onAccessRevoked?: (address: string) => void
}
export function AccessControl({ onAccessGranted, onAccessRevoked }: AccessControlProps)
```

From src/app/page.tsx (current state):
```typescript
export default function Home()
// Uses: useAccount from wagmi, ConnectButton from @rainbow-me/rainbowkit
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import and render UploadButton and AccessControl components</name>
  <files>src/app/page.tsx</files>
  <action>
    Modify src/app/page.tsx to integrate both components:

    1. Add imports at the top:
       - `import { UploadButton } from '@/components/UploadButton'`
       - `import { AccessControl } from '@/components/AccessControl'`

    2. Add optional callback handlers in Home component:
       - `const handleUploadComplete = (cid: string, url: string) => { console.log('Upload complete:', cid, url) }`
       - `const handleAccessGranted = (address: string) => { console.log('Access granted:', address) }`
       - `const handleAccessRevoked = (address: string) => { console.log('Access revoked:', address) }`

    3. Render components after the wallet connection status div:
       - Wrap in a container div with proper spacing (className="mt-8 space-y-6")
       - Add section headers for clarity:
         - "Upload Neural Data" section with UploadButton
         - "Manage Access" section with AccessControl
       - Pass userId prop to UploadButton (can use wallet address or default 'user-001')
       - Pass callback handlers to both components

    4. Only show these sections when isConnected is true (wrap in {isConnected && (...)} block)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - UploadButton component imported and rendered when wallet connected
    - AccessControl component imported and rendered when wallet connected
    - Both components have proper props passed
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm build` - completes without TypeScript or build errors
2. Run `pnpm dev` and visit http://localhost:3000 - after connecting wallet, page shows:
   - "Upload Neural Data" section with "Upload File" and "Generate Mock EEG" buttons
   - "Manage Access" section with researcher address input and Grant/Revoke Access buttons
</verification>

<success_criteria>
- src/app/page.tsx imports both UploadButton and AccessControl components
- Both components render when wallet is connected
- Build passes with no errors
- UAT Tests 3 (View Upload UI) and 7 (Grant Access UI) now pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ipfs-integration/02-05-SUMMARY.md` documenting:
- Component integration changes made
- Screenshot or description of updated homepage layout
- Confirmation that both UAT gaps are closed
</output>
