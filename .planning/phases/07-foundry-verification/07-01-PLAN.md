---
phase: 7
plan: 07-01
name: Foundry Setup & Contract Testing
wave: 1
depends_on: []
requirements:
  - CONTRACT-06
files_modified:
  - contracts/foundry.toml
  - contracts/src/NeuralDataRegistry.sol
  - contracts/test/NeuralDataRegistry.t.sol
autonomous: false
must_haves:
  truths:
    - Foundry toolchain installed and accessible via `forge --version`
    - NeuralDataRegistry.sol compiles without errors
    - All Foundry tests pass with `forge test`
  artifacts:
    - path: contracts/src/NeuralDataRegistry.sol
      description: Smart contract implementation
    - path: contracts/test/NeuralDataRegistry.t.sol
      description: Foundry test suite
    - path: contracts/foundry.toml
      description: Foundry configuration
  key_links:
    - test -> contract: tests verify NeuralDataRegistry functionality
---

<task>
<name>Install Foundry Toolchain</name>
<files>
  - system: foundryup
  - system: forge
</files>
<action>
1. Install Foundry toolchain by running `curl -L https://foundry.paradigm.xyz | bash`
2. Run `foundryup` to install latest Foundry version
3. Restart terminal/shell to load new PATH
4. Verify installation with `forge --version`
</action>
<verify>
  <automated>forge --version</automated>
  <expected>forge version output (e.g., "forge 1.6.0")</expected>
</verify>
<done>Foundry installed and `forge --version` returns version number</done>
</task>

<task>
<name>Compile NeuralDataRegistry Contract</name>
<files>
  - contracts/src/NeuralDataRegistry.sol
  - contracts/foundry.toml
</files>
<action>
1. Navigate to contracts/ directory
2. Run `forge build` to compile all contracts
3. If compilation errors occur, fix them in NeuralDataRegistry.sol
4. Verify contract compiles without warnings
</action>
<verify>
  <automated>forge build</automated>
  <expected>Compilation successful with no errors</expected>
</verify>
<done>Contract compiles successfully, output shows NeuralDataRegistry.json in out/</done>
</task>

<task>
<name>Create and Run Foundry Tests</name>
<files>
  - contracts/test/NeuralDataRegistry.t.sol
  - contracts/src/NeuralDataRegistry.sol
</files>
<action>
1. Create test file contracts/test/NeuralDataRegistry.t.sol if not exists
2. Implement tests for: uploadData, grantAccess, revokeAccess, hasAccess
3. Run `forge test -vvv` to execute tests with verbose output
4. Fix any failing tests until all pass
</action>
<verify>
  <automated>forge test -vvv</automated>
  <expected>All tests passed</expected>
</verify>
<done>All Foundry tests pass, test coverage meets requirements</done>
</task>
