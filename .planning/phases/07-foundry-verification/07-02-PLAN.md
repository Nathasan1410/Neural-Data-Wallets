---
phase: 7
plan: 07-02
name: Contract Deployment & Verification
wave: 2
depends_on:
  - 07-01
requirements:
  - CONTRACT-01
files_modified:
  - contracts/script/Deploy.s.sol
  - contracts/foundry.toml
  - .env.local
  - src/lib/contracts/neuralDataRegistry.ts
autonomous: false
must_haves:
  truths:
    - Contract deployed to Base Sepolia testnet
    - Contract source code verified on BaseScan explorer
    - Deployed address recorded in .env.local and frontend config
  artifacts:
    - path: contracts/script/Deploy.s.sol
      description: Deployment script for NeuralDataRegistry
    - path: contracts/foundry.toml
      description: Foundry config with Base Sepolia RPC
  key_links:
    - deploy -> contract: deployment script deploys NeuralDataRegistry
    - config -> deploy: foundry.toml configures deployment target
    - frontend -> deploy: src/lib/contracts/neuralDataRegistry.ts uses deployed address
---

<task>
<name>Create Deployment Script</name>
<files>
  - contracts/script/Deploy.s.sol
  - contracts/src/NeuralDataRegistry.sol
</files>
<action>
1. Create contracts/script/Deploy.s.sol if not exists
2. Import forge-std Script.sol and console.sol
3. Create Deploy script that deploys NeuralDataRegistry
4. Include constructor arguments if any
</action>
<verify>
  <automated>forge build --contracts script/Deploy.s.sol</automated>
  <expected>Deployment script compiles successfully</expected>
</verify>
<done>Deploy.s.sol created and compiles successfully</done>
</task>

<task>
<name>Configure Base Sepolia RPC</name>
<files>
  - contracts/foundry.toml
  - .env.local
</files>
<action>
1. Add Base Sepolia RPC configuration to foundry.toml
2. Set ETHERSCAN_API_KEY and BASESCAN_API_KEY in .env.local
3. Set PRIVATE_KEY for deployment account in .env.local
4. Verify configuration with `forge config --chain 84532`
</action>
<verify>
  <automated>forge config --chain 84532</automated>
  <expected>Configuration shows Base Sepolia settings</expected>
</verify>
<done>Base Sepolia RPC configured, environment variables set</done>
</task>

<task>
<name>Deploy Contract to Base Sepolia</name>
<files>
  - contracts/script/Deploy.s.sol
  - .env.local
</files>
<action>
1. Run deployment: `forge script script/Deploy.s.sol --rpc-url https://sepolia.base.org --broadcast --verify`
2. Wait for deployment transaction to confirm
3. Record deployed contract address from output
4. Update .env.local with NEURAL_DATA_REGISTRY_ADDRESS
5. Update src/lib/contracts/neuralDataRegistry.ts with deployed address
</action>
<verify>
  <automated>forge script script/Deploy.s.sol --rpc-url https://sepolia.base.org --broadcast --dry-run</automated>
  <expected>Deployment simulation successful, gas estimation shown</expected>
</verify>
<done>Contract deployed to Base Sepolia, address recorded and updated in frontend</done>
</task>

<task>
<name>Verify Contract on BaseScan</name>
<files>
  - contracts/src/NeuralDataRegistry.sol
  - .env.local
</files>
<action>
1. If not verified during deployment, run: `forge verify-contract {ADDRESS} src/NeuralDataRegistry.sol --chain 84532 --watch`
2. Alternatively, use BaseScan web UI at https://sepolia.basescan.org/
3. Provide contract address, source code, compiler version, and constructor arguments
4. Confirm verification status shows as "Verified" on BaseScan
</action>
<verify>
  <automated>cast code $NEURAL_DATA_REGISTRY_ADDRESS --rpc-url https://sepolia.base.org | grep -q .</automated>
  <manual>Visit https://sepolia.basescan.org/address/{CONTRACT_ADDRESS} and confirm "Contract Source Code Verified" badge visible</manual>
  <expected>Contract bytecode exists on-chain AND source code tab shows verified contract with matching bytecode</expected>
</verify>
<done>Contract verified on BaseScan explorer, source code publicly viewable</done>
</task>
