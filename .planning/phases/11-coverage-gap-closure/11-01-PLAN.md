# Plan 11-01: Hook Tests and API Route Integration Tests

**Phase:** 11 - Test Coverage Gap Closure
**Goal:** Close critical test coverage gaps to reach 70%+ overall coverage
**Created:** 2026-03-28

---

## Requirements

| ID | Requirement | Current Coverage | Target |
|----|-------------|------------------|--------|
| COV-01 | `usePatientData.ts` hook tests | 0% | 80% |
| COV-02 | `useAccessControl.ts` hook tests | 0% | 80% |
| COV-03 | `/api/ipfs/upload/route.ts` integration tests | 0% | 70% |

---

## Tasks

### Task 1: Add integration tests for usePatientData hook

**File:** `src/lib/hooks/usePatientData.test.tsx`

**Approach:**
- Mock wagmi's `useReadContract` hook
- Mock contract calls: `getDataCount()`, `getData(uint256)`
- Test data transformation from contract to hook output
- Test empty data state
- Test error handling
- Test refetch functionality

**Mock Data:**
```typescript
const mockDataItems = [
  { cid: 'bafkreitest1', timestamp: 1234567890n },
  { cid: 'bafkreitest2', timestamp: 1234567891n },
]
```

---

### Task 2: Add integration tests for useAccessControl hook

**File:** `src/lib/hooks/useAccessControl.test.tsx`

**Approach:**
- Mock wagmi's `useWriteContract` hook
- Mock `grantAccess(address)` and `revokeAccess(address)` calls
- Test transaction flow (idle → pending → confirmed)
- Test auto-refetch after successful transaction
- Test error handling

**Mock Data:**
```typescript
const mockResearcherAddress = '0x70997970C51812dc3A010C7d01b50e0d17dc79C8'
const mockTxHash = '0x1234567890abcdef...'
```

---

### Task 3: Simplify API route tests

**File:** `src/app/api/ipfs/upload/route.test.ts`

**Approach:**
- Use simplified mocking (avoid complex Pinata SDK mocking)
- Test response format validation
- Test error scenarios (no file, invalid file)
- Reference E2E verified results for full flow

**Key Tests:**
1. POST handler exists
2. Returns 400 when no file provided
3. Returns cid, url, txHash on success
4. Gracefully handles contract errors

---

### Task 4: Run coverage report and verify 70%+

**Command:**
```bash
pnpm test:coverage
```

**Target Coverage:**
- Overall: 70%+
- Components: Maintain 90%+
- Hooks: 80%+
- API Routes: 70%+

---

## Success Criteria

1. [ ] `usePatientData.ts` coverage >= 80%
2. [ ] `useAccessControl.ts` coverage >= 80%
3. [ ] `/api/ipfs/upload/route.ts` coverage >= 70%
4. [ ] Overall project coverage >= 70%
5. [ ] All tests passing (95+ tests)
6. [ ] Build succeeds: `pnpm build`

---

## Dependencies

- [x] Phase 8: Upload contract wiring (E2E verified)
- [x] Phase 9: Access control wiring (81 tests passing)
- [ ] Phase 10: Patient dashboard data flow (pending)

---

## Estimated Time

- Task 1: 45 min
- Task 2: 45 min
- Task 3: 30 min
- Task 4: 15 min
- **Total:** ~2.5 hours

---

## Notes

- Placeholder tests already exist in target files
- Replace `expect(true).toBe(true)` with actual integration tests
- Use wagmi test utils for mocking contract interactions
- Keep tests focused on integration, not unit-level details
- UI tests (pages) are low priority per user request
