---
phase: 01-smart-contract-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified: [contracts/script/Deploy.s.sol, contracts/.env.example]
autonomous: false
requirements: [CONTRACT-01]

must_haves:
  truths:
    - "Contract deployed to Base Sepolia or Sepolia testnet"
    - "Deployment script is reusable and documented"
    - "Deployed contract address is recorded and verifiable"
    - "Contract is verified on block explorer"
  artifacts:
    - path: "contracts/script/Deploy.s.sol"
      provides: "Foundry deployment script for testnet"
      exports: ["run() function"]
    - path: "contracts/.env.example"
      provides: "Environment variable template"
      contains: "PRIVATE_KEY, RPC_URL"
  key_links:
    - from: "contracts/script/Deploy.s.sol"
      to: "contracts/src/NeuralDataRegistry.sol"
      via: "import and deploy"
      pattern: "new NeuralDataRegistry"
---

<objective>
Deploy NeuralDataRegistry.sol to Base Sepolia or Sepolia testnet and verify contract on block explorer.

Purpose: Make contract accessible for frontend integration in subsequent phases.
Output: Deployed contract address, verified source code, deployment documentation.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Deployment script interface for Foundry -->
<!-- Executor: Create deployment script that can be re-run if needed -->

```solidity
// Deployment script structure
contract DeployScript is Script {
    function run() external {
        // Load private key from env
        uint256 deployerPrivateKey = vm.envUint("PRIVATE_KEY");

        // Start broadcast
        vm.startBroadcast(deployerPrivateKey);

        // Deploy contract
        NeuralDataRegistry registry = new NeuralDataRegistry();

        // Stop broadcast
        vm.stopBroadcast();

        // Output deployment info
        console.log("Deployed NeuralDataRegistry at:", address(registry));
        console.log("Network:", block.chainid);
    }
}
```
</interfaces>

<user_setup>
  - service: testnet-deployment
    why: "Deploy smart contract to Base Sepolia or Sepolia testnet"
    env_vars:
      - name: PRIVATE_KEY
        source: "MetaMask or create new wallet - export private key (NEVER commit to git)"
      - name: RPC_URL
        source: "Chainlist.org - Base Sepolia RPC or use Alchemy/Infura"
    dashboard_config:
      - task: "Fund deployer wallet with testnet ETH"
        location: "Base Sepolia faucet or Sepolia faucet"
      - task: "Verify contract on block explorer"
        location: "basescan.org or etherscan.io - verify source code"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment script and environment configuration</name>
  <files>contracts/script/Deploy.s.sol, contracts/.env.example</files>
  <action>
    Create Foundry deployment infrastructure:

    1. Create Deploy.s.sol with:
       - Import forge-std/Script.sol and source contract
       - run() function that:
         * Loads PRIVATE_KEY from environment (vm.envUint)
         * Loads RPC endpoint from environment
         * Uses vm.startBroadcast() to begin deployment
         * Deploys NeuralDataRegistry
         * Uses vm.stopBroadcast() to end
         * Logs deployed address and network chain ID

    2. Create .env.example with:
       - PRIVATE_KEY=your_deployer_private_key
       - RPC_URL=https://sepolia.base.org (Base Sepolia) or Sepolia RPC
       - ETHERSCAN_API_KEY=optional_for_verification

    3. Update foundry.toml with:
       - rpc_endpoints configuration
       - etherscan configuration for verification
  </action>
  <verify>
    <automated>cd contracts && forge script script/Deploy.s.sol --dry-run</automated>
  </verify>
  <done>Deployment script ready, environment template created</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision: Choose target testnet for deployment</name>
  <decision>Select deployment network: Base Sepolia vs Sepolia</decision>
  <context>
    Phase 1 goal specifies "Base Sepolia or Sepolia testnet". Decision affects:
    - RPC endpoint configuration
    - Block explorer for verification
    - Frontend wagmi configuration in Phase 3

    Both are viable - Base Sepolia recommended for L2 cost benefits and alignment with BASE ecosystem.
  </context>
  <options>
    <option id="base-sepolia">
      <name>Base Sepolia (Recommended)</name>
      <pros>
        - L2 = lower gas costs for testing
        - Coinbase ecosystem alignment
        - Faster block times
        - Free faucet available
      </pros>
      <cons>
        - Slightly less established than Sepolia
        - May need bridge for testnet ETH
      </cons>
    </option>
    <option id="sepolia">
      <name>Sepolia (Alternative)</name>
      <pros>
        - Ethereum L1 testnet - most widely supported
        - Multiple faucets available
        - Well-documented
      </pros>
      <cons>
        - Higher gas costs than L2
        - Slower block times
      </cons>
    </option>
  </options>
  <resume-signal>Select: base-sepolia or sepolia</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Deploy contract to testnet</name>
  <action>
    CRITICAL: This task requires human verification for security. Claude will prepare commands, human executes.

    Human must:
    1. Create .env file from .env.example (NEVER commit .env to git)
    2. Fund deployer wallet using testnet faucet
    3. Run deployment command

    Claude prepares commands based on user's network selection.
  </action>
  <instructions>
    After network selection, execute:

    1. Create .env file:
       cp .env.example .env
       # Edit .env with your PRIVATE_KEY and RPC_URL

    2. Fund wallet at faucet:
       - Base Sepolia: https://cloud.google.com/application/web3/faucet/Base Sepolia
       - Sepolia: https://sepoliafaucet.com/

    3. Deploy (Claude will provide exact command after network decision)

    4. After deployment, human must verify contract on block explorer:
       - Base Sepolia: https://sepolia.basescan.org/
       - Sepolia: https://sepolia.etherscan.io/
  </instructions>
  <resume-signal>
    Type "deployed" with contract address after deployment completes, e.g.:
    "deployed 0x1234...5678"
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] Deployment script exists and compiles
- [ ] .env.example created with required variables
- [ ] Contract deployed to selected testnet
- [ ] Contract address recorded in deployment summary
- [ ] Contract verified on block explorer (source code visible)
</verification>

<success_criteria>
1. Deploy.s.sol script created and working
2. Contract deployed to Base Sepolia or Sepolia
3. Contract address recorded in SUMMARY
4. Contract source verified on block explorer
5. .env.example provided for future deployments
</success_criteria>

<output>
After completion, create .planning/phases/01-smart-contract-foundation/01-03-SUMMARY.md with:
- Deployed contract address
- Network (Base Sepolia or Sepolia)
- Block explorer URL
- Deployment transaction hash
- Contract ABI (from artifacts)
- Instructions for Phase 2 integration
</output>
