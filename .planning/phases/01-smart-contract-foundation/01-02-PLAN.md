---
phase: 01-smart-contract-foundation
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified: [contracts/test/NeuralDataRegistry.t.sol]
autonomous: true
requirements: [CONTRACT-06, ACCESS-04, ACCESS-05]

must_haves:
  truths:
    - "All contract functions have passing tests"
    - "Test coverage is 100% for NeuralDataRegistry.sol"
    - "Access control revert behavior verified"
    - "Event emission verified for all state-changing functions"
  artifacts:
    - path: "contracts/test/NeuralDataRegistry.t.sol"
      provides: "Comprehensive Foundry tests for all contract functions"
      contains: "contract NeuralDataRegistryTest"
  key_links:
    - from: "contracts/test/NeuralDataRegistry.t.sol"
      to: "contracts/src/NeuralDataRegistry.sol"
      via: "import NeuralDataRegistry"
      pattern: "import.*NeuralDataRegistry"
---

<objective>
Write comprehensive Foundry tests for NeuralDataRegistry.sol covering all functions, edge cases, and access control behavior.

Purpose: Verify CONTRACT-06 (all tests pass) and ensure access control works correctly before deployment.
Output: Test file with 100% coverage, all tests passing.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
</context>

<interfaces>
<!-- Contract interface from Plan 01-01 - tests will validate these behaviors -->
<!-- Executor: Write tests that verify each function and event -->

```solidity
// Test setup: Foundry test contract inheriting Test from forge-std
contract NeuralDataRegistryTest is Test {
    NeuralDataRegistry public registry;
    address public owner = address(1);
    address public researcher = address(2);
    address public unauthorized = address(3);

    // Test all functions from NeuralDataRegistry
    function uploadData(string memory cid) external returns (uint256);
    function grantAccess(address researcher) external;
    function revokeAccess(address researcher) external;
    function hasAccess(address user, address researcher) external view returns (bool);
    function getDataByOwner(address owner) external view returns (Data[] memory);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Write test scaffolding and setup</name>
  <files>contracts/test/NeuralDataRegistry.t.sol</files>
  <action>
    Create test file with Foundry test structure:
    1. Import forge-std/Test.sol and source contract
    2. Create NeuralDataRegistryTest contract inheriting Test
    3. Define test fixtures: registry, owner, researcher, unauthorized addresses
    4. Implement setUp() function to deploy fresh contract before each test
    5. Add helper function to create mock CID strings for testing
  </action>
  <verify>
    <automated>cd contracts && forge build --contracts test/NeuralDataRegistry.t.sol</automated>
  </verify>
  <done>Test file compiles, setUp() creates fresh contract instance for each test</done>
</task>

<task type="auto">
  <name>Task 1: Test uploadData function and DataRegistered event</name>
  <files>contracts/test/NeuralDataRegistry.t.sol</files>
  <action>
    Write tests for uploadData:
    1. test_uploadData(): Upload CID, verify stored correctly with correct timestamp
    2. test_uploadData_emitsEvent(): Verify DataRegistered event emitted with correct parameters
    3. test_uploadData_multipleData(): Upload multiple CIDs, verify all stored
    4. test_uploadData_gasEstimate(): Log gas cost for documentation

    Use vm.prank(owner) to simulate different callers
    Use vm.expectEmit() to verify event emission
  </action>
  <verify>
    <automated>cd contracts && forge test --match-test "test_uploadData" -vvv</automated>
  </verify>
  <done>All uploadData tests pass, event emission verified</done>
</task>

<task type="auto">
  <name>Task 2: Test grantAccess and revokeAccess functions</name>
  <files>contracts/test/NeuralDataRegistry.t.sol</files>
  <action>
    Write tests for access control functions:
    1. test_grantAccess(): Owner grants access to researcher, hasAccess returns true
    2. test_grantAccess_emitsEvent(): Verify AccessGranted event emitted
    3. test_revokeAccess(): Owner revokes access, hasAccess returns false
    4. test_revokeAccess_emitsEvent(): Verify AccessRevoked event emitted
    5. test_grantAccess_revokeAccess(): Grant then revoke, final state is false
    6. test_grantAccess_differentResearchers(): Grant to multiple researchers independently
  </action>
  <verify>
    <automated>cd contracts && forge test --match-test "test_grantAccess|test_revokeAccess" -vvv</automated>
  </verify>
  <done>All grant/revoke tests pass, access state correctly updated</done>
</task>

<task type="auto">
  <name>Task 3: Test hasAccess view function and ACCESS-04 enforcement</name>
  <files>contracts/test/NeuralDataRegistry.t.sol</files>
  <action>
    Write tests for access control enforcement:
    1. test_hasAccess_defaultFalse(): hasAccess returns false without explicit grant
    2. test_hasAccess_afterGrant(): hasAccess returns true after grant
    3. test_hasAccess_afterRevoke(): hasAccess returns false after revoke
    4. test_hasAccess_selfAccess(): Owner always has access to own data
    5. test_getDataByOwner_ownerCanAccess(): Owner can retrieve own data (no revert)
    6. test_getDataByOwner_grantedCanAccess(): Researcher with grant can access (no revert)
    7. test_getDataByOwner_unauthorizedReverts(): Unauthorized address causes revert (ACCESS-04)
       Use vm.expectRevert() to verify the revert
  </action>
  <verify>
    <automated>cd contracts && forge test --match-test "test_hasAccess|test_getDataByOwner" -vvv</automated>
  </verify>
  <done>Access control tests pass, unauthorized access correctly reverts</done>
</task>

</tasks>

<verification>
- [ ] forge test runs with all tests passing
- [ ] Test coverage report shows 100% for NeuralDataRegistry.sol
- [ ] Event emission tests verify all 3 events
- [ ] ACCESS-04 revert behavior tested and verified
- [ ] No test warnings or skipped tests
</verification>

<success_criteria>
1. Test file NeuralDataRegistry.t.sol created with 15+ tests
2. All tests pass with `forge test`
3. Event emission verified for uploadData, grantAccess, revokeAccess
4. ACCESS-04 enforcement verified (unauthorized access reverts)
5. Test coverage is 100% for contract functions
6. Gas estimates logged for documentation
</success_criteria>

<output>
After completion, create .planning/phases/01-smart-contract-foundation/01-02-SUMMARY.md with:
- Total test count
- Test coverage percentage
- Gas estimates for key functions
- Any edge cases discovered during testing
</output>
