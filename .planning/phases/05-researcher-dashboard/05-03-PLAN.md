---
phase: 05-researcher-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: [src/app/researcher/page.tsx, src/app/researcher/page.test.tsx]
autonomous: false
requirements: [RES-01, RES-02, RES-03]
must_haves:
  truths:
    - "Researcher dashboard accessible at /researcher route"
    - "Wallet connection required to view data"
    - "Access denied error shown when no grants exist"
  artifacts:
    - path: "src/app/researcher/page.tsx"
      provides: "Main dashboard page"
      min_lines: 40
    - path: "src/app/researcher"
      provides: "Route directory"
      contains: "page.tsx"
  key_links:
    - from: "src/app/researcher/page.tsx"
      to: "src/lib/hooks/useResearcherData.ts"
      via: "hook import"
      pattern: "useResearcherData"
    - from: "src/app/researcher/page.tsx"
      to: "src/components/AccessibleDataList.tsx"
      via: "component import"
      pattern: "AccessibleDataList"
---

<objective>
Create researcher dashboard page at /researcher route
Purpose: Main page integrating hook and components for researcher view
Output: Complete researcher dashboard with wallet connection check and data display
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-researcher-dashboard/05-RESEARCH.md

# Reference existing patterns from Phase 4
@src/app/patient/page.tsx
@src/app/patient/page.test.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/hooks/useResearcherData.ts (05-01):
```typescript
export function useResearcherData() {
  accessibleData: AccessibleData[]
  isLoading: boolean
  error: string | null
  ipfsLoading: Record<string, boolean>
}
```

From src/components/AccessibleDataList.tsx (05-02):
```typescript
interface AccessibleDataListProps {
  data: AccessibleData[]
  isLoading: boolean
  error: string | null
  ipfsLoading: Record<string, boolean>
}
```

Navigation pattern from src/app/patient/page.tsx:
```typescript
<nav>
  <Link href="/">Home</Link> |
  <Link href="/patient">Patient</Link> |
  <Link href="/researcher">Researcher</Link>
</nav>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create researcher page</name>
  <files>src/app/researcher/page.tsx, src/app/researcher/page.test.tsx</files>
  <behavior>
    - Test: Page renders loading state before mount
    - Test: Page shows "Connect wallet" message when not connected
    - Test: Page shows ConnectButton when not connected
    - Test: Page shows dashboard when wallet connected
    - Test: Navigation links present (Home, Patient, Researcher)
    - Test: useResearcherData hook called when connected
    - Test: AccessibleDataList receives correct props
    - Test: Page title is "Researcher Dashboard"
  </behavior>
  <action>
    Create src/app/researcher/page.tsx following patient/page.tsx pattern:

    1. Client component ('use client' directive)
    2. Import ConnectButton from @rainbow-me/rainbowkit
    3. Import useAccount from wagmi
    4. Import useResearcherData from hooks
    5. Import AccessibleDataList from components
    6. Import Link from next/link

    States:
    1. Not mounted: Show [LOADING] (hydration guard)
    2. Not connected: Show "Connect wallet" message + ConnectButton + nav
    3. Connected: Show dashboard header + AccessibleDataList

    Dashboard sections:
    - Header: "Researcher Dashboard"
    - Nav: Home | Patient | Researcher
    - Section: "Accessible Neural Data" with description
    - AccessibleDataList component with hook data

    Create matching test file with testids: loading, connect-wallet, dashboard, data-section
  </action>
  <verify>
    <automated>npm test -- --filter=page.test</automated>
  </verify>
  <done>Researcher page renders with proper states and navigation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add access denied error handling for RES-03</name>
  <files>src/lib/hooks/useResearcherData.ts, src/app/researcher/page.tsx</files>
  <behavior>
    - Test: Hook handles contract revert with "Access denied" message
    - Test: UI displays access denied message to user
    - Test: Toast notification shows on access denied (using react-hot-toast)
  </behavior>
  <action>
    Enhance useResearcherData hook to handle access denied:

    1. In the useEffect that fetches IPFS data, wrap individual getData calls in try/catch
    2. Check if error message includes "Access denied"
    3. Set ipfsError: 'Access denied' for that record
    4. Use react-hot-toast to show error notification if ALL data access fails

    For RES-03 verification:
    - If getAllAccessibleData returns empty array, show "No data accessible"
    - If individual items have access denied, mark those items with ipfsError
    - Show toast: toast.error('Some data inaccessible - access denied by contract')

    Note: getAllAccessibleData already filters to only accessible data, so
    access denied would only occur if:
    - Contract read fails (network issue)
    - Direct getData call attempted on non-accessible data
  </action>
  <verify>
    <automated>npm test -- --filter=useResearcherData</automated>
  </verify>
  <done>Access denied errors handled gracefully with user feedback</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify researcher dashboard end-to-end</name>
  <what-built>
    Complete researcher dashboard at /researcher route with:
    - useResearcherData hook fetching from contract
    - AccessibleDataList displaying CIDs and IPFS data
    - Access denied error handling
  </what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:3000/researcher
    3. Verify "Connect wallet" message shows when disconnected
    4. Connect wallet (MetaMask/Coinbase)
    5. Verify dashboard loads with accessible data
    6. If no data accessible, verify "No data accessible" message
    7. If patient has granted access, verify:
       - CID displays in table
       - Timestamp formatted correctly
       - IPFS data preview shows
       - "View on IPFS" link works
    8. Test with different wallet (no access) - verify access denied handling
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard works, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] Page renders at /researcher route
- [ ] Wallet connection states work correctly
- [ ] Accessible data displays when available
- [ ] Access denied errors handled gracefully
- [ ] Navigation works between pages
- [ ] All tests passing
</verification>

<success_criteria>
1. /researcher route accessible and functional
2. Shows connect wallet state when disconnected
3. Shows accessible data when connected with grants
4. Shows "access denied" or empty state appropriately
5. IPFS data displays correctly
6. Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/05-researcher-dashboard/05-03-SUMMARY.md`
</output>
