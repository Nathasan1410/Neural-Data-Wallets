---
phase: 05-researcher-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/contracts/neuralDataRegistry.ts, src/lib/hooks/useResearcherData.ts, src/lib/hooks/useResearcherData.test.tsx]
autonomous: true
requirements: [RES-01, RES-02]
must_haves:
  truths:
    - "Contract ABI includes getAllAccessibleData function"
    - "Hook returns accessible data list when wallet connected"
    - "IPFS data fetched from Pinata gateway for each CID"
  artifacts:
    - path: "src/lib/contracts/neuralDataRegistry.ts"
      provides: "Complete ABI with getAllAccessibleData"
      contains: "getAllAccessibleData"
    - path: "src/lib/hooks/useResearcherData.ts"
      provides: "Data fetching hook"
      exports: ["useResearcherData", "AccessibleData"]
  key_links:
    - from: "src/lib/hooks/useResearcherData.ts"
      to: "src/lib/contracts/neuralDataRegistry.ts"
      via: "ABI import"
      pattern: "NEURAL_DATA_ABI"
    - from: "src/lib/hooks/useResearcherData.ts"
      to: "Pinata gateway"
      via: "fetch() API"
      pattern: "gateway\\.pinata\\.cloud"
---

<objective>
Add ABI definitions and create useResearcherData hook for fetching accessible data
Purpose: Enable researcher dashboard to query smart contract for accessible CIDs and fetch IPFS data
Output: Updated ABI with getAllAccessibleData, data fetching hook with IPFS integration
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-researcher-dashboard/05-RESEARCH.md

# Reference existing patterns from Phase 4
@src/lib/hooks/usePatientData.ts
@src/lib/hooks/usePatientData.test.tsx
@src/lib/contracts/neuralDataRegistry.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/hooks/usePatientData.ts:
```typescript
export interface DataRecord {
  dataId: bigint
  cid: string
  timestamp: bigint
  metadata: string
  ipfsData?: any
  ipfsError?: string
}

// Pattern: useReadContract with useEffect for IPFS fetching
const { data, isLoading, error } = useReadContract({
  address: NEURAL_DATA_CONTRACT,
  abi: NEURAL_DATA_ABI,
  functionName: 'getDataCount',
  query: { retry: 2, retryDelay: 1000 }
})
```

From src/lib/contracts/neuralDataRegistry.ts:
```typescript
export const NEURAL_DATA_CONTRACT = process.env.NEXT_PUBLIC_NEURAL_DATA_CONTRACT as Address
export const NEURAL_DATA_ABI = [...] // Needs getAllAccessibleData added
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getAllAccessibleData to ABI</name>
  <files>src/lib/contracts/neuralDataRegistry.ts</files>
  <behavior>
    - Test: NEURAL_DATA_ABI includes getAllAccessibleData function definition
    - Test: getAllAccessibleData accepts address parameter and returns [uint256[], (string, uint256, bytes)[]]
    - Test: TypeScript compilation succeeds without ABI errors
  </behavior>
  <action>
    Add getAllAccessibleData function definition to NEURAL_DATA_ABI array in src/lib/contracts/neuralDataRegistry.ts.

    Function signature from NeuralDataRegistry.sol:166-189:
    - Input: researcher (address)
    - Output: dataIds (uint256[]), dataList (tuple[] with cid, timestamp, metadata)

    Also add getDataByOwnerPaginated and hasAccessToData for completeness (identified in 05-RESEARCH.md ABI gap section).

    After adding, verify TypeScript compiles without errors: npm run build
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>NEURAL_DATA_ABI includes getAllAccessibleData, getDataByOwnerPaginated, hasAccessToData functions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useResearcherData hook</name>
  <files>src/lib/hooks/useResearcherData.ts, src/lib/hooks/useResearcherData.test.tsx</files>
  <behavior>
    - Test: Hook returns { accessibleData, isLoading, error, ipfsLoading }
    - Test: Hook calls getAllAccessibleData with user address when connected
    - Test: Hook fetches IPFS data for each CID from Pinata gateway
    - Test: Hook handles access denied errors gracefully
    - Test: Hook handles IPFS fetch errors gracefully
  </behavior>
  <action>
    Create useResearcherData.ts following the pattern from usePatientData.ts:

    1. Use useAccount() to get connected wallet address
    2. Use useReadContract to call getAllAccessibleData(userAddress)
    3. Parse result: [dataIds, dataList] where dataList contains {cid, timestamp, metadata}
    4. In useEffect, iterate through data items and fetch IPFS content from Pinata gateway
    5. Use env var NEXT_PUBLIC_PINATA_GATEWAY (fallback: 'gateway.pinata.cloud')
    6. Gateway URL format: https://{gateway}/ipfs/{cid}
    7. Handle errors: access denied, IPFS 404, JSON parse failures
    8. Store ipfsData and ipfsError for each record

    Create matching test file useResearcherData.test.tsx with mocks for:
    - wagmi useReadContract
    - useAccount returning mock address
    - fetch() for IPFS gateway calls
  </action>
  <verify>
    <automated>npm test -- --filter=useResearcherData</automated>
  </verify>
  <done>Hook created with tests passing, fetches accessible data and IPFS content</done>
</task>

</tasks>

<verification>
- [ ] NEURAL_DATA_ABI includes getAllAccessibleData function
- [ ] useResearcherData hook exported from hooks directory
- [ ] Hook tests pass (>80% coverage)
- [ ] Build succeeds without TypeScript errors
</verification>

<success_criteria>
1. TypeScript compiles with new ABI definitions
2. Hook returns accessible data array when wallet connected
3. IPFS data fetched and stored in hook state
4. Error handling for access denied and IPFS failures
5. Tests pass with >80% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-researcher-dashboard/05-01-SUMMARY.md`
</output>
