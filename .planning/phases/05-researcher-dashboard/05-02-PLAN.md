---
phase: 05-researcher-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/AccessibleDataList.tsx, src/components/AccessibleDataList.test.tsx, src/components/IPFSDataViewer.tsx]
autonomous: true
requirements: [RES-01, RES-02]
must_haves:
  truths:
    - "Researcher sees table of accessible CIDs"
    - "IPFS data preview displays for each record"
    - "Loading and error states visible to user"
  artifacts:
    - path: "src/components/AccessibleDataList.tsx"
      provides: "Data table component"
      min_lines: 50
    - path: "src/components/IPFSDataViewer.tsx"
      provides: "JSON data display"
      exports: ["IPFSDataViewer"]
  key_links:
    - from: "src/components/AccessibleDataList.tsx"
      to: "Pinata gateway"
      via: "getIpfsGatewayUrl function"
      pattern: "gateway\\.pinata\\.cloud"
---

<objective>
Create UI components for displaying researcher accessible data and IPFS content
Purpose: Visual display of accessible CIDs with IPFS data preview
Output: AccessibleDataList table component and IPFSDataViewer for JSON display
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-researcher-dashboard/05-RESEARCH.md

# Reference existing patterns from Phase 4
@src/components/UploadedDataList.tsx
@src/components/UploadedDataList.test.tsx
@src/lib/hooks/useResearcherData.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/hooks/useResearcherData.ts (created in 05-01):
```typescript
export interface AccessibleData {
  dataId: bigint
  cid: string
  timestamp: bigint
  metadata: string
  ipfsData?: any
  ipfsError?: string
}

// Hook return:
{
  accessibleData: AccessibleData[]
  isLoading: boolean
  error: string | null
  ipfsLoading: Record<string, boolean>
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AccessibleDataList component</name>
  <files>src/components/AccessibleDataList.tsx, src/components/AccessibleDataList.test.tsx</files>
  <behavior>
    - Test: Component renders loading state when isLoading=true
    - Test: Component renders error message when error is not null
    - Test: Component renders empty state when data array is empty
    - Test: Component renders table with data rows when data exists
    - Test: CID is truncated (6 chars...4 chars)
    - Test: Timestamp formatted as locale date string
    - Test: IPFS gateway link opens in new tab
    - Test: ipfsLoading state displays "Loading..." for fetching items
    - Test: ipfsError displays error message in red
    - Test: ipfsData displays JSON preview (first 100 chars)
  </behavior>
  <action>
    Create AccessibleDataList.tsx following UploadedDataList.tsx pattern:

    Props: { data, isLoading, error, ipfsLoading }

    Render states:
    1. Loading: "Loading accessible data..."
    2. Error: "Error: {error}"
    3. Empty: "No data accessible. Ask patients to grant you access."
    4. Data table with columns: ID, CID, Timestamp, IPFS Data, Actions

    Features:
    - Truncate CID: cid.slice(0,6) + '...' + cid.slice(-4)
    - Format timestamp: new Date(Number(timestamp) * 1000).toLocaleString()
    - IPFS gateway link: https://{NEXT_PUBLIC_PINATA_GATEWAY}/ipfs/{cid}
    - Show ipfsLoading per item
    - Show ipfsError in red text
    - Show ipfsData as JSON preview (first 100 chars with ...)
    - "View on IPFS" external link

    Create matching test file with testids: loading, error, empty, data-table
  </action>
  <verify>
    <automated>npm test -- --filter=AccessibleDataList</automated>
  </verify>
  <done>Component renders accessible data table with all states handled</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create IPFSDataViewer component</name>
  <files>src/components/IPFSDataViewer.tsx, src/components/IPFSDataViewer.test.tsx</files>
  <behavior>
    - Test: Component renders JSON data in expandable/collapsible format
    - Test: Component handles nested EEG data structure (samples, channels, metadata)
    - Test: Component shows "No data available" for null/undefined
    - Test: Component shows parse error for invalid JSON
  </behavior>
  <action>
    Create IPFSDataViewer.tsx for displaying EEG data:

    Props: { data: any, loading?: boolean, error?: string }

    Features:
    1. Loading state: skeleton or "Loading data..."
    2. Error state: display error message
    3. Data display:
       - Use <pre> with JSON.stringify(data, null, 2)
       - Add max-height with overflow for large data
       - Syntax highlighting optional (deferred if complex)

    EEG data structure expected:
    ```json
    {
      "timestamp": "2026-03-28T...",
      "duration": 30,
      "sampleRate": 256,
      "channels": ["Fp1", "Fp2", "F3", "F4"],
      "samples": [[...], [...]],
      "metadata": { "patient": "...", "task": "..." }
    }
    ```

    Create simple test file verifying render states
  </action>
  <verify>
    <automated>npm test -- --filter=IPFSDataViewer</automated>
  </verify>
  <done>Component displays EEG JSON data with proper formatting</done>
</task>

</tasks>

<verification>
- [ ] AccessibleDataList renders all states (loading, error, empty, data)
- [ ] IPFSDataViewer displays JSON data correctly
- [ ] Component tests pass with >80% coverage
- [ ] Build succeeds without TypeScript errors
</verification>

<success_criteria>
1. AccessibleDataList displays table with CID, timestamp, IPFS preview
2. External IPFS link works
3. IPFSDataViewer handles nested EEG data structure
4. All component tests passing
5. Components follow existing Tailwind styling patterns
</success_criteria>

<output>
After completion, create `.planning/phases/05-researcher-dashboard/05-02-SUMMARY.md`
</output>
