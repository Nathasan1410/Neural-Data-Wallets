---
phase: 8
name: Wire Upload to Contract Storage
type: gap-closure
status: pending
started: null
completed: null
---

## Goal

Connect the existing IPFS upload API route to the smart contract so that uploaded CIDs are stored on-chain.

## Requirements Closed

- IPFS-02: System stores returned CID in smart contract linked to user address

## Integration Gaps Closed

- Upload API → Contract `uploadData()` call

## Tasks

### 1. Add viem Server-Side
- Install viem if not already present
- Create server-side wallet configuration
- Store deployer private key in `.env.local`

### 2. Modify Upload API Route
- Edit `src/app/api/ipfs/upload/route.ts`
- After `pinata.upload.file()`, add contract write call
- Call `uploadData(cid)` on NeuralDataRegistry contract
- Wait for transaction receipt

### 3. Return Tx Hash to Client
- Update response to include `txHash` alongside `cid` and `url`
- Handle errors gracefully (contract revert, gas issues)

### 4. Test End-to-End
- Upload file via frontend
- Verify CID appears in contract via `getDataCount()` and `getData()`
- Confirm `usePatientData` hook receives the data

## Success Criteria

- [ ] Upload API writes CID to contract after IPFS upload
- [ ] Transaction hash returned to client
- [ ] `getDataCount()` increments after upload
- [ ] Patient dashboard displays uploaded data

## Dependencies

- Phase 7: Foundry Setup (contract must be deployed)

## Blocks

- Phase 10: Patient Dashboard Data Flow

## Files to Modify

- `src/app/api/ipfs/upload/route.ts`
- `.env.local` (add deployer private key)
- `src/lib/contracts/neuralDataRegistry.ts` (may need write ABI)
