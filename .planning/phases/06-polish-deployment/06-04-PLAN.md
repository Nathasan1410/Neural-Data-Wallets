---
phase: 06-polish-deployment
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified: [vercel.json, .env.example, playwright.config.ts, tests/e2e/flow.spec.ts, tests/e2e/responsive.spec.ts, package.json]
autonomous: false
requirements: [DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "Vercel build succeeds with environment variables configured"
    - "E2E test can navigate through full user flow"
    - "E2E test verifies mobile responsive layout"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: "routes or build configuration"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "NEXT_PUBLIC_ variables"
    - path: "playwright.config.ts"
      provides: "Playwright E2E test configuration"
      exports: ["default"]
    - path: "tests/e2e/flow.spec.ts"
      provides: "Full user flow E2E test"
      contains: "test.describe with wallet mock"
    - path: "tests/e2e/responsive.spec.ts"
      provides: "Mobile viewport test"
      contains: "test.describe with viewport tests"
  key_links:
    - from: "tests/e2e/flow.spec.ts"
      to: "src/app/page.tsx"
      via: "data-testid selectors"
      pattern: "data-testid=.*"
    - from: "vercel.json"
      to: "next.config.js"
      via: "Next.js build config"
      pattern: "buildCommand|outputDirectory"
---

<objective>
Configure Vercel deployment and create E2E tests for full flow validation

Purpose: Production deployment requires Vercel configuration and environment variables. E2E tests validate the complete user flow works end-to-end before demo/pitch.

Output: Vercel config, .env.example, Playwright setup, E2E flow tests
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-polish-deployment/06-RESEARCH.md

# Contract address already deployed
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Install Playwright dependencies</name>
  <files>package.json</files>
  <action>
    Run: pnpm add -D @playwright/test. Then run: pnpm exec playwright install (browsers). This adds E2E testing capability to the project.
  </action>
  <verify>
    <automated>pnpm exec playwright --version</automated>
  </verify>
  <done>Playwright installed with Chromium browser</done>
</task>

<task type="auto">
  <name>Task 1: Create Vercel configuration</name>
  <files>vercel.json, .env.example</files>
  <action>
    Create vercel.json with Next.js build settings. Create .env.example with required variables: NEXT_PUBLIC_PINATA_GATEWAY, NEXT_PUBLIC_NEURAL_DATA_CONTRACT (0x2700C2B1268B115cF06136b881341903aBC7DC4a), NEXT_PUBLIC_CHAIN_ID (84532), NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID (placeholder). Document that Vercel needs these configured in dashboard.
  </action>
  <verify>
    <automated>pnpm next build</automated>
  </verify>
  <done>vercel.json and .env.example created, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright configuration</name>
  <files>playwright.config.ts</files>
  <action>
    Create playwright.config.ts with: testDir: 'tests/e2e', use: headless: true, viewport: { width: 1280, height: 720 }, projects: [{ name: 'chromium', use: { browserName: 'chromium' } }, { name: 'mobile', use: { viewport: { width: 375, height: 667 } } }]. Add webServer config for local dev testing.
  </action>
  <verify>
    <automated>pnpm exec playwright show-config</automated>
  </verify>
  <done>Playwright config created with desktop and mobile projects</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide E2E wallet mocking strategy</name>
  <decision>How to mock wallet connection in E2E tests</decision>
  <context>
    Playwright cannot use real browser extension wallets (MetaMask) in headless mode. Need mocking strategy.
  </context>
  <options>
    <option id="simple-mock">
      <name>Simple window.ethereum mock</name>
      <pros>Quick to implement, no extra dependencies, works for basic flows</pros>
      <cons>Doesn't test real RainbowKit integration, manual state management</cons>
    </option>
    <option id="synpress">
      <name>@synthetixio/synpress library</name>
      <pros>Real MetaMask mocking, wagmi-specific, well-maintained</pros>
      <cons>Extra dependency, larger bundle, overkill for hackathon demo</cons>
    </option>
  </options>
  <resume-signal>Select: simple-mock or synpress</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Create E2E flow test</name>
  <files>tests/e2e/flow.spec.ts</files>
  <action>
    Create flow.spec.ts with test suite "Neural Data Wallet E2E Flow". Test 1: "patient can upload data" - mock wallet, navigate to /, connect, click generate mock, verify upload success. Test 2: "researcher can view accessible data" - mock different wallet, navigate to /researcher, verify data section visible. Use data-testid selectors from existing components.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/flow.spec.ts --headed</automated>
  </verify>
  <done>E2E flow test created and runs (may fail on real wallet - expected until mock complete)</done>
</task>

<task type="auto">
  <name>Task 5: Create responsive E2E test</name>
  <files>tests/e2e/responsive.spec.ts</files>
  <action>
    Create responsive.spec.ts with mobile viewport tests. Test: "homepage renders at mobile viewport" - set viewport to 375x667, navigate to /, verify key elements visible. Test: "tables scroll horizontally on mobile" - verify overflow-x-auto wrapper exists. Use Playwright's page.setViewportSize().
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/responsive.spec.ts --headed</automated>
  </verify>
  <done>Responsive test created and passes at mobile viewport</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify Vercel deployment</name>
  <what-built>
    Vercel configuration (vercel.json) and .env.example created. Playwright E2E tests added.
  </what-built>
  <how-to-verify>
    1. Push code to GitHub repository
    2. Go to vercel.com and import this repository
    3. Configure environment variables from .env.example
    4. Deploy to production
    5. Verify deployment URL works (wallet connects, can navigate pages)

    If Vercel account not available, run locally: pnpm next build && pnpm next start
  </how-to-verify>
  <resume-signal>Type "deployed" when Vercel deployment is live, or describe any build errors</resume-signal>
</task>

</tasks>

<verification>
- Playwright installed and configured
- vercel.json created with Next.js settings
- .env.example documents all required variables
- E2E flow test covers upload and view flows
- Responsive test validates mobile viewport
- Vercel deployment successful (or local build succeeds)
</verification>

<success_criteria>
1. pnpm exec playwright test runs without config errors
2. vercel.json present in repo root
3. .env.example has all NEXT_PUBLIC_ variables
4. flow.spec.ts has 2+ tests covering critical user flows
5. responsive.spec.ts validates mobile viewport
6. Vercel build succeeds (or local build succeeds)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-deployment/06-04-SUMMARY.md`
</output>
