---
phase: 06-polish-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/UploadButton.tsx, src/app/page.tsx, src/app/patient/page.tsx]
autonomous: true
requirements: [DEPLOY-05, DEPLOY-03]

must_haves:
  truths:
    - "User sees toast notification when upload succeeds"
    - "User sees toast notification when upload fails"
    - "Upload success triggers parent callback with CID and txHash"
  artifacts:
    - path: "src/components/UploadButton.tsx"
      provides: "Upload button with toast feedback"
      exports: ["UploadButton"]
    - path: "src/app/page.tsx"
      provides: "Homepage with wired upload callback"
      contains: "handleUploadComplete with toast"
  key_links:
    - from: "src/components/UploadButton.tsx"
      to: "react-hot-toast"
      via: "toast.success/toast.error calls"
      pattern: "toast\\.(success|error)"
---

<objective>
Wire toast notifications to UploadButton component for upload transaction feedback

Purpose: Complete the error handling and notification consistency across all async operations. UploadButton currently has no user-visible feedback on success/failure.

Output: UploadButton.tsx with toast integration, homepage callbacks updated
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-polish-deployment/06-RESEARCH.md

# Existing toast pattern from GrantAccessButton (reference for consistency)
@src/components/GrantAccessButton.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add toast tests for UploadButton</name>
  <files>src/components/UploadButton.test.tsx</files>
  <behavior>
    - Test 1: Shows success toast when upload completes with txHash
    - Test 2: Shows success toast when upload completes without txHash (IPFS only)
    - Test 3: Shows error toast when upload API returns error
    - Test 4: Shows error toast when network error occurs
    - Test 5: Calls onUploadComplete callback with cid and url on success
  </behavior>
  <action>
    Create test file following existing component test patterns in src/components/. Use vitest, @testing-library/react. Mock fetch for API calls. Assert toast calls using react-hot-toast mock.
  </action>
  <verify>
    <automated>pnpm test -- src/components/UploadButton.test.tsx --run</automated>
  </verify>
  <done>Test file created with 5 tests, all failing initially (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire toast to UploadButton</name>
  <files>src/components/UploadButton.tsx</files>
  <behavior>
    - Import toast from react-hot-toast
    - Call toast.success() with txHash-aware message
    - Call toast.error() on upload failure
    - Maintain existing onUploadComplete callback
  </behavior>
  <action>
    Add toast import. In handleUpload and handleGenerateAndUpload, add toast.success() after successful response with message: "Data uploaded and stored on-chain!" if txHash present, else "Data uploaded to IPFS (contract storage pending)". Add toast.error() in catch block with error message. Keep existing onUploadComplete callback for parent component handling.
  </action>
  <verify>
    <automated>pnpm test -- src/components/UploadButton.test.tsx --run</automated>
  </verify>
  <done>UploadButton shows toast on success/failure, all tests pass (GREEN phase)</done>
</task>

<task type="auto">
  <name>Task 3: Wire upload callbacks on homepage</name>
  <files>src/app/page.tsx, src/app/patient/page.tsx</files>
  <action>
    Update handleUploadComplete in both pages to show toast notifications. Homepage: import toast, update callback to show "Data uploaded!" toast. Patient page: current callback only refetches - add toast.success() before refetch. This provides redundant toast safety (component + parent).
  </action>
  <verify>
    <automated>pnpm next build</automated>
  </verify>
  <done>Both pages have toast callbacks, build succeeds with no errors</done>
</task>

</tasks>

<verification>
- UploadButton shows success toast with appropriate message
- UploadButton shows error toast on failure
- Homepage and patient page callbacks also show toasts (redundant but safe)
- All existing tests still pass
- New UploadButton tests pass
</verification>

<success_criteria>
1. Toast notifications appear for upload success/failure
2. Messages differentiate on-chain vs IPFS-only storage
3. Error messages are user-friendly and descriptive
4. All 5 new tests pass
5. Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-deployment/06-01-SUMMARY.md`
</output>
