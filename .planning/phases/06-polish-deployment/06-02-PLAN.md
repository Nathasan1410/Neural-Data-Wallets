---
phase: 06-polish-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/UploadButton.tsx, src/components/UploadedDataList.tsx, src/components/AccessibleDataList.tsx, src/app/patient/page.tsx, src/app/researcher/page.tsx]
autonomous: true
requirements: [DEPLOY-06]

must_haves:
  truths:
    - "User sees visual spinner during upload transaction"
    - "User sees visual spinner during data loading"
    - "Loading state includes both spinner icon and text label"
  artifacts:
    - path: "src/components/UploadButton.tsx"
      provides: "Upload button with spinner loading indicator"
      contains: "animate-spin"
    - path: "src/components/UploadedDataList.tsx"
      provides: "Data list with loading spinner"
      exports: ["UploadedDataList"]
    - path: "src/components/AccessibleDataList.tsx"
      provides: "Researcher data list with loading spinner"
      exports: ["AccessibleDataList"]
  key_links:
    - from: "src/components/*.tsx"
      to: "Tailwind animate-spin"
      via: "CSS utility classes"
      pattern: "animate-spin.*border-"
---

<objective>
Add visual loading spinners to all async operations for better UX feedback

Purpose: Current loading states show text only ("Loading...", "Uploading..."). Visual spinners provide clearer feedback and match the polish level expected for production demos.

Output: Enhanced loading states with Tailwind spinners across all components
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-polish-deployment/06-RESEARCH.md

# Loading state pattern from RevokeAccessButton (reference)
@src/components/RevokeAccessButton.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create loading state test utilities</name>
  <files>src/test/loadingTestUtils.ts</files>
  <behavior>
    - Export function to assert spinner is visible
    - Export function to assert loading text is present
    - Export function to wait for loading to complete
    - Use data-testid attributes for reliable selectors
  </behavior>
  <action>
    Create test utility file with helper functions for testing loading states. Export renderSpinnerMatcher, assertLoadingVisible, assertLoadingHidden, waitForLoadingComplete. Use @testing-library/react queries.
  </action>
  <verify>
    <automated>pnpm test -- src/test/loadingTestUtils.test.ts --run</automated>
  </verify>
  <done>Utility file created with exported helpers, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add spinner to UploadButton</name>
  <files>src/components/UploadButton.tsx, src/components/UploadButton.test.tsx</files>
  <behavior>
    - Show spinner + text when uploading state is true
    - Spinner uses Tailwind animate-spin class
    - Both buttons (Upload File, Generate Mock) show loading state
  </behavior>
  <action>
    Add spinner JSX inside both buttons: <div className="flex items-center gap-2"> with animate-spin div (h-4 w-4 border-2 border-current border-t-transparent rounded-full) + text span. Update button text to "Uploading..." and "Generating..." during loading. Update tests to assert spinner visibility.
  </action>
  <verify>
    <automated>pnpm test -- src/components/UploadButton.test.tsx --run</automated>
  </verify>
  <done>UploadButton shows spinner + text when uploading, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add spinner to UploadedDataList</name>
  <files>src/components/UploadedDataList.tsx, src/components/UploadedDataList.test.tsx</files>
  <behavior>
    - Show centered spinner when isLoading prop is true
    - Show error message when error prop is present
    - Show empty state when data is empty array
  </behavior>
  <action>
    Add loading state check at top of component: if isLoading return spinner container with animate-spin. Add error state: if error return error message. Ensure data table renders only when data exists and not loading. Update tests for all three states.
  </action>
  <verify>
    <automated>pnpm test -- src/components/UploadedDataList.test.tsx --run</automated>
  </verify>
  <done>UploadedDataList shows spinner during load, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add spinner to AccessibleDataList</name>
  <files>src/components/AccessibleDataList.tsx, src/components/AccessibleDataList.test.tsx</files>
  <behavior>
    - Show spinner when isLoading or ipfsLoading is true
    - Differentiate between contract loading and IPFS loading states
    - Show combined loading message
  </behavior>
  <action>
    Add loading state check: if isLoading return "Loading accessible data..." with spinner. If ipfsLoading return "Fetching data from IPFS..." with spinner. Add both states with appropriate messages. Update tests to verify both loading states.
  </action>
  <verify>
    <automated>pnpm test -- src/components/AccessibleDataList.test.tsx --run</automated>
  </verify>
  <done>AccessibleDataList shows spinner for both load states, tests pass</done>
</task>

</tasks>

<verification>
- All components show visual spinner during loading
- Loading text is descriptive (not generic "Loading...")
- Spinner uses consistent Tailwind animate-spin pattern
- All new and existing tests pass
- Build succeeds with no TypeScript errors
</verification>

<success_criteria>
1. UploadButton shows spinner during upload
2. UploadedDataList shows spinner during contract data fetch
3. AccessibleDataList shows spinner for contract and IPFS loading
4. All loading states have data-testid for E2E tests
5. At least 8 new test assertions added
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-deployment/06-02-SUMMARY.md`
</output>
