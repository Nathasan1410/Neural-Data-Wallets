---
phase: 06-polish-deployment
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified: [src/app/page.tsx, src/app/patient/page.tsx, src/app/researcher/page.tsx, src/components/UploadedDataList.tsx, src/components/AccessibleDataList.tsx]
autonomous: true
requirements: [DEPLOY-04]

must_haves:
  truths:
    - "Page layout adapts to mobile viewport (320px wide)"
    - "Tables scroll horizontally on mobile without breaking layout"
    - "Header stacks vertically on mobile, horizontal on desktop"
    - "Buttons are touch-friendly size on mobile (min 44px height)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Responsive homepage layout"
      contains: "sm: md: lg: breakpoint classes"
    - path: "src/app/patient/page.tsx"
      provides: "Responsive patient dashboard"
      contains: "responsive container classes"
    - path: "src/app/researcher/page.tsx"
      provides: "Responsive researcher dashboard"
      contains: "mobile-first layout classes"
    - path: "src/components/UploadedDataList.tsx"
      provides: "Responsive table with overflow-x-auto wrapper"
      exports: ["UploadedDataList"]
    - path: "src/components/AccessibleDataList.tsx"
      provides: "Responsive table with mobile support"
      exports: ["AccessibleDataList"]
  key_links:
    - from: "src/app/*.tsx"
      to: "Tailwind responsive breakpoints"
      via: "sm: md: lg: class prefixes"
      pattern: "(sm|md|lg):"
---

<objective>
Add responsive design classes to all pages and components for mobile support

Purpose: Demo users may access on mobile devices. All pages must work on viewports from 320px (iPhone SE) to 1920px (desktop). Tailwind's mobile-first utilities enable rapid responsive development.

Output: All pages and table components with proper responsive classes
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-polish-deployment/06-RESEARCH.md

# Responsive pattern examples from research
See: 06-RESEARCH.md - Responsive Design Pattern: Tailwind Breakpoints
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add responsive wrapper to homepage</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - Container has responsive padding (p-4 on mobile, p-8 on md+)
    - Header stacks on mobile, row on sm+
    - Max-width container centers content on large screens
    - Sections have proper spacing on all viewports
  </behavior>
  <action>
    Update main container: className="min-h-screen p-4 md:p-8". Update header: flex flex-col sm:flex-row sm:items-center sm:justify-between. Update sections: space-y-6 with responsive heading sizes (text-xl md:text-2xl). Ensure ConnectButton is accessible on mobile.
  </action>
  <verify>
    <automated>pnpm next build</automated>
  </verify>
  <done>Homepage builds successfully with responsive classes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add responsive layout to patient dashboard</name>
  <files>src/app/patient/page.tsx</files>
  <behavior>
    - Page container has responsive padding
    - Navigation links wrap on mobile
    - Heading sizes scale with viewport
    - Upload button section is touch-friendly
  </behavior>
  <action>
    Add wrapper div with p-4 md:p-8. Update nav to flex-wrap with gap-2. Add responsive heading classes. Ensure button has min-h-[44px] for touch targets. Add max-w-6xl mx-auto container.
  </action>
  <verify>
    <automated>pnpm next build</automated>
  </verify>
  <done>Patient dashboard builds with responsive layout</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add responsive layout to researcher dashboard</name>
  <files>src/app/researcher/page.tsx</files>
  <action>
    Apply same responsive patterns as patient dashboard: p-4 md:p-8 container, flex-wrap nav, responsive headings, touch-friendly buttons. Ensure data-testid attributes preserved for E2E tests.
  </action>
  <verify>
    <automated>pnpm next build</automated>
  </verify>
  <done>Researcher dashboard builds with responsive layout</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add responsive table wrappers</name>
  <files>src/components/UploadedDataList.tsx, src/components/AccessibleDataList.tsx</files>
  <behavior>
    - Tables wrapped in overflow-x-auto container
    - Table has min-w-full class
    - Horizontal scroll available on mobile
    - Data-testid="data-table" on wrapper for E2E tests
  </behavior>
  <action>
    Wrap table in <div className="overflow-x-auto" data-testid="data-table">. Add min-w-full to table class. Test horizontal scroll behavior in mobile viewport. Preserve all existing functionality.
  </action>
  <verify>
    <automated>pnpm test -- src/components/UploadedDataList.test.tsx src/components/AccessibleDataList.test.tsx --run</automated>
  </verify>
  <done>Tables scroll horizontally on mobile, tests pass</done>
</task>

</tasks>

<verification>
- All three pages have responsive containers
- Tables have overflow-x-auto wrappers
- Navigation wraps on mobile viewports
- Touch targets meet 44px minimum
- Build succeeds with no warnings
</verification>

<success_criteria>
1. Homepage works at 320px, 768px, 1920px viewports
2. Patient dashboard responsive at all breakpoints
3. Researcher dashboard responsive at all breakpoints
4. Tables scroll horizontally without layout break
5. All data-testid attributes preserved for E2E
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-deployment/06-03-SUMMARY.md`
</output>
